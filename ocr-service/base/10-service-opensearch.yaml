apiVersion: v1
kind: Service
metadata:
  name: opensearch
  namespace: ocr-service
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: opensearch
  ports:
    - name: rest
      port: 9200
      targetPort: rest
    - name: metrics
      port: 9600
      targetPort: metrics
