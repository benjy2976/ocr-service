apiVersion: v1
kind: Service
metadata:
  name: ocr-api
  namespace: ocr-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ocr-api
  ports:
    - name: http
      port: 8000
      targetPort: http
